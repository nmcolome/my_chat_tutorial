<%= form_for(@private_chat) do |f| %>
  <%= f.collection_select(:participant_ids, User.all.order(:username), :id, :username, {}, name: "participant_ids[]", multiple: true, size: 10) %>
  <%= f.submit "Go" %>
<% end %>
